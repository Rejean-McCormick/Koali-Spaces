export default function Loading() {
  return (
    <section aria-busy="true" aria-live="polite" style={{ padding: 24 }}>
      <h2>Loading interface</h2>
      <p>Loading the validated local Space and its admitted presentation contributions.</p>
    </section>
  );
}
