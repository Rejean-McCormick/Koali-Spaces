import ModulePageShell from '@/components/shell/ModulePageShell'; export default async function ModuleSurface({params}:{params:Promise<{moduleId:string;route?:string[]}>}){const {moduleId,route}=await params;return <ModulePageShell title={moduleId} description="Admitted module surface"><p>Logical route: {route?.join('/')||'home'}. The owner-provided local surface is resolved through its admitted module/asset manifest; Koali Spaces does not recreate the owner's business implementation.</p></ModulePageShell>}
