import {Alert,Space,Typography} from 'antd'; import type {ReactNode} from 'react'; export default function ModulePageShell({title,description,state='ready',primaryAction,secondaryActions,children}:{title:string;description?:ReactNode;state?:string;primaryAction?:ReactNode;secondaryActions?:ReactNode;children:ReactNode}){return <section style={{maxWidth:1200,margin:'0 auto',padding:24}}><Space direction="vertical" size="large" style={{width:'100%'}}><div style={{display:'flex',gap:16,alignItems:'flex-start',flexWrap:'wrap'}}><div style={{flex:1,minWidth:220}}><Typography.Title level={2} style={{marginBottom:4}}>{title}</Typography.Title>{description&&<Typography.Paragraph type="secondary">{description}</Typography.Paragraph>}</div><Space wrap>{secondaryActions}{primaryAction}</Space></div>{state!=='ready'&&<Alert type={state==='error'?'error':'warning'} showIcon message={`Interface state: ${state}`}/>}<div>{children}</div></Space></section>}
