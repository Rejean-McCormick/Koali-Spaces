import fs from 'node:fs';import path from 'node:path';const roots=['src','public','interface'];const bad=[];for(const root of roots){if(!fs.existsSync(root))continue;const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.(ts|tsx|js|mjs|css|json|html)$/.test(p)){const s=fs.readFileSync(p,'utf8');if(/https?:\/\//.test(s)&&!p.includes('schema'))bad.push(p)}}};walk(root)}if(bad.length){console.error('remote runtime URL references:',bad);process.exit(1)}console.log('PASS: no public remote runtime asset references');
